apiVersion: v1
kind: Service
metadata:
  name: notifiarr-bridge
  namespace: notifiarr-bridge
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: notifiarr-bridge
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
