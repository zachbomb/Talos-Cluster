apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helm-release.yaml
  - volumeSnapshotClass.yaml
  - ingress.yaml
  # jobs directory commented out until CRDs are stable
  # - jobs

# Optional: Add health checks to ensure HelmRelease is ready before applying dependent resources
# Uncomment these lines if you want Flux to wait for Longhorn to be healthy
# healthChecks:
#   - apiVersion: helm.toolkit.fluxcd.io/v2
#     kind: HelmRelease
#     name: longhorn
#     namespace: longhorn-system