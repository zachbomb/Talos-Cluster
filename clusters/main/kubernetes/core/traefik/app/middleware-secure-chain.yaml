apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: secure-chain
  namespace: traefik
spec:
  chain:
    middlewares:
      # bouncer temporarily removed - LAPI connectivity issue causes 403 on all traffic
      # re-add once CrowdSec TLS + bouncer connectivity is verified:
      # - name: bouncer
      #   namespace: traefik
      - name: local-whitelist
        namespace: traefik
