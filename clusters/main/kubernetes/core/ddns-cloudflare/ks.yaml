apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ddns-cloudflare
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/core/ddns-cloudflare/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster
